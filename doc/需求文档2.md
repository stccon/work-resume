- 因为简历中有个头像字段，检查下ai对话里有没有地方把头像字段也传给ai了。要避免把头像字段传给ai,因为那是base64，消耗很多token
- 上传头像的按钮，改到简历名称附近。
  头像，目前是所有简历公用一个头像。现在要修改，把头像跟简历一一对应。
- 菜单栏的，File Edit View Window Help这些暂时不要了，只显示版本号即可，目前版本号是1.0.0
- 设置里，暂时不用支持Apikey的填写
- 右上角那个选择视觉模板的下拉框，打开的时候好像是透明的，因为能看到后面的内容，这样影响使用。
- 对话框输入文字的地方，有点小，感觉可以稍微调大一点。
- 修改视觉模板的时候，整个应用的字体都变了。这不合理，按理说只是简历变才对。