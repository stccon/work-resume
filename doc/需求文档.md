- 一个通过跟ai聊天生成建立的pc端工具
- 基于opencode二次开发，需要把opencode打包进应用里，避免用户安装和配置
- 默认使用opencode的免费模型，在软件窗口可以切换模型
- 软件的主窗口左边是目录结构，右边是聊天窗口
- 初期功能，先实现聊天功能，聊天内容需要有思考模式
- 预置几个简历模板，我目前没有模板，你先帮我生成几个，预留扩展空间，后期可以替换成精美模板
- 支持用户上传简历，根据上传的简历进行优化。
- 主动询问用户信息，根据多轮对话，获得用户信息后，生成简历
- 一定要采用可以开发精美界面的技术栈
- 暂时先这样，后面再补充